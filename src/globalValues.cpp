#include "globalValues.h"

bool quitGame = false;

int currentHorizontalResolution = DEFAULT_HORIZONTAL_RESOLUTION;
int currentVerticalResolution = DEFAULT_VERTICAL_RESOLUTION;
int currentScreen = TITLE_MENU_SCREEN;